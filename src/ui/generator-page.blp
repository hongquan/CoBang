using Gtk 4.0;

template $GeneratorPage: Box {
  $GeneratorStartingPage starting_page {
    visible: bind $is_inputing_regular_text(template.generator-state) as <bool>;
  }

  $GeneratorQRCodePage qr_code_page {
    visible: bind $is_qr_code_generated(template.generator-state) as <bool>;
  }

}
