using Gtk 4.0;
using Adw 1;

template $GeneratorPage: Box {
    Adw.StatusPage {
        title: _("Generator");
        description: _("Will be available in next versions...");
        hexpand: true;
    }
}
